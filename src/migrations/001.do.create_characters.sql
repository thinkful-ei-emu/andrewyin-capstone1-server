CREATE TABLE characters {
  charId PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  charName TEXT NOT NULL,
  charRace TEXT NOT NULL,
  charClass TEXT NOT NULL,
  charDesc TEXT,
  date_created TIMESTAMP DEFAULT now() NOT NULL
};